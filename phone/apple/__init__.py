"""Чтение данных Apple из первоисточников.

Здесь нет ярлыков и нет ручной сборки чего бы то ни было. Есть четыре
источника, из которых данные забираются целиком:

* `health_export` — выгрузка «Здоровье → Экспортировать медданные»: вся
  история, все метрики, кольца активности, тренировки. Один файл, один раз.
* `auto_export` — приложение Health Auto Export шлёт те же метрики дальше
  само, по расписанию. Настройка — выбрать «все метрики» и вписать адрес.
* `chatdb` и `callhistory` — базы Mac: переписка и журнал звонков, которые
  iCloud кладёт туда с iPhone. Читаются напрямую, без участия человека.
* `backup` — те же базы, но из резервной копии iPhone, если Mac нет.

Модули пакета держатся на одной стандартной библиотеке: агент запускается
на Mac, где ставить зависимости незачем и негде.
"""
